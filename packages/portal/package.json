{
    "name": "portal",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "clean": "rimraf build",
        "start": "react-app-rewired start",
        "watch": "yarn start",
        "build": "react-app-rewired build",
        "build:dev": "",
        "build:prod": "react-app-rewired build",
        "eject": "react-scripts eject",
        "test": "react-app-rewired test"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": {
        "production": [
            "chrome >= 51",
            "firefox >= 57",
            "safari >= 10",
            "edge >= 79"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version",
            "last 1 edge version"
        ]
    },
    "dependencies": {
        "prop-types": "^15.7.2",
        "react": "^16.12.0",
        "react-dom": "^16.12.0",
        "react-scripts": "3.3.0",
        "@netbrain/component": "0.0.0"
    }
}

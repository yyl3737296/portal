(function(window) {
    var NetBrain = window.NetBrain || {};
    window.NetBrain = NetBrain;

    NetBrain.Portal = NetBrain.Portal || {};
    NetBrain.Portal.Config = {
        BaseUrl: '/portal',
    };

    NetBrain.Portal.HelpLink = {
        Support: 'https://www.netbraintech.com/netbrain-support/',
    };
})(window);
